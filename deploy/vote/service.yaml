apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote
spec:
  ports:
  - name: 32300-80
    port: 80
    protocol: TCP
    targetPort: 32300
  selector:
    app: vote
  type: NodePort
