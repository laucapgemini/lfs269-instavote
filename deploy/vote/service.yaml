apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote
spec:
  ports:
  - name: 30300-80
    port: 30300
    protocol: TCP
    targetPort: 80
  selector:
    app: vote
  type: NodePort
